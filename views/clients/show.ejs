<%- include('../partials/head') -%>

<body class="d-flex flex-column vh-100 ">
    <%- include('../partials/navbar') -%>

<main class="container mt-5">

    <div class="card" >
        <div class="card-body">
          <h2 class="card-title"><%= c.firstName %> <%= c.lastName %></h5>
          <h6 class="card-subtitle mb-2 text-muted"></h6>
          <div class="pt-6">
            <a href="/clients/<%-c._id%>/update"><button class="btn btn-primary">Edit</button></a>
            <form action="/investments/?_method=DELETE" method="POST">
              <button class="btn btn-danger">Delete</button>
            </form>
            
          </div>
        </div>
      </div>


</main>
    
</body>

<%- include('../partials/footer') -%>